{% extends 'base.html.twig' %}

{% block title %}Mee{% endblock %}

{% block body %}
    <h1>Mee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mee.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mee_index') }}">back to list</a>

    <a href="{{ path('mee_edit', {'id': mee.id}) }}">edit</a>

    {{ include('mee/_delete_form.html.twig') }}
{% endblock %}
