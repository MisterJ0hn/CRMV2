{% extends 'base.html.twig' %}

{% block title %}{{pagina|default('')}}{% endblock %}

{% block body %}
<div class="card">
    <div class="card-body">
        <form action="{{path('app_ticket_search')}}" method="POST">
            <div class="row">
                <div class="col-sm-6 col-md-3">
                    <input type="text" placeholder="Folio" name="txtFolio" class="form-control">
                </div>
                <div class="col-sm-6 col-md-3">
                    <input type="text" placeholder="Nombre/Rut" name="txtNombreRut" class="form-control">
                </div>
                <div class="col-sm-12 col-md-3">
                    <button class="btn btn-primary"><i class="fas fa-search"></i></button>
                </div>

            </div>
        </form>
    </div>
</div>
{{ include("ticket/_cabezera.html.twig") }}

{{ include('ticket/_form.html.twig',{'contrato_id': contrato.id}) }}

   
{% endblock %}
