{% extends 'base.html.twig' %}

{% block title %}Sucursal{% endblock %}

{% block body %}
    <h1>Sucursal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sucursal.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sucursal.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sucursal_index') }}">back to list</a>

    <a href="{{ path('sucursal_edit', {'id': sucursal.id}) }}">edit</a>

    {{ include('sucursal/_delete_form.html.twig') }}
{% endblock %}
