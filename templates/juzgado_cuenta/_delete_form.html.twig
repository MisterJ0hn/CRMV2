<form method="post" action="{{ path('juzgado_cuenta_delete', {'id': juzgado_cuentum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ juzgado_cuentum.id) }}">
    <button class="btn">Delete</button>
</form>
