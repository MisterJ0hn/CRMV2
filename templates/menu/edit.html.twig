{% extends 'base.html.twig' %}

{% block title %}Edit Menu{% endblock %}

{% block body %}
   <div class="card">
        <div class="card-header">
        Editar item para Men&uacute; {{menuCabezera.nombre}}
        </div>

        <div class="card-body">
        {{ include('menu/_form.html.twig', {'button_label': 'Modificar'}) }}

    
                   <div class="col-2"> {{ include('menu/_delete_form.html.twig') }}</div>
                   </div>
        </div>
    </div>


{% endblock %}
