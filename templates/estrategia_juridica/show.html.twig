{% extends 'base.html.twig' %}

{% block title %}EstrategiaJuridica{% endblock %}

{% block body %}
    <h1>EstrategiaJuridica</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ estrategia_juridica.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ estrategia_juridica.Nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('estrategia_juridica_index') }}">back to list</a>

    <a href="{{ path('estrategia_juridica_edit', {'id': estrategia_juridica.id}) }}">edit</a>

    {{ include('estrategia_juridica/_delete_form.html.twig') }}
{% endblock %}
