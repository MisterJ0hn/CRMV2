
{% extends 'base.html.twig' %}

{% block title %}{{pagina|default('Usuarios')}}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
    {{ include('cobradores/_form.html.twig') }}

        </div>
    </div>
{% endblock %}
