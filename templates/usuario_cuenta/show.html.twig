{% extends 'base.html.twig' %}

{% block title %}UsuarioCuenta{% endblock %}

{% block body %}
    <h1>UsuarioCuenta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario_cuentum.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuario_cuenta_index') }}">back to list</a>

    <a href="{{ path('usuario_cuenta_edit', {'id': usuario_cuentum.id}) }}">edit</a>

    {{ include('usuario_cuenta/_delete_form.html.twig') }}
{% endblock %}
