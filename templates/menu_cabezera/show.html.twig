{% extends 'base.html.twig' %}

{% block title %}MenuCabezera{% endblock %}

{% block body %}
    <h1>MenuCabezera</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_cabezera.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ menu_cabezera.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('menu_cabezera_index') }}">back to list</a>

    <a href="{{ path('menu_cabezera_edit', {'id': menu_cabezera.id}) }}">edit</a>

    {{ include('menu_cabezera/_delete_form.html.twig') }}
{% endblock %}
