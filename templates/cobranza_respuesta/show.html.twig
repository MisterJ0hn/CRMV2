{% extends 'base.html.twig' %}

{% block title %}CobranzaRespuesta{% endblock %}

{% block body %}
    <h1>CobranzaRespuesta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cobranza_respuestum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cobranza_respuestum.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cobranza_respuesta_index') }}">back to list</a>

    <a href="{{ path('cobranza_respuesta_edit', {'id': cobranza_respuestum.id}) }}">edit</a>

    {{ include('cobranza_respuesta/_delete_form.html.twig') }}
{% endblock %}
