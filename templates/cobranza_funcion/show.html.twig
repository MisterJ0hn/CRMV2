{% extends 'base.html.twig' %}

{% block title %}CobranzaFuncion{% endblock %}

{% block body %}
    <h1>CobranzaFuncion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cobranza_funcion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ cobranza_funcion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cobranza_funcion_index') }}">back to list</a>

    <a href="{{ path('cobranza_funcion_edit', {'id': cobranza_funcion.id}) }}">edit</a>

    {{ include('cobranza_funcion/_delete_form.html.twig') }}
{% endblock %}
