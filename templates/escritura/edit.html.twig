{% extends 'base.html.twig' %}

{% block title %}Edit Escritura{% endblock %}

{% block body %}
    <h1>Edit Escritura</h1>

    {{ include('escritura/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('escritura_index') }}">back to list</a>

    {{ include('escritura/_delete_form.html.twig') }}
{% endblock %}
