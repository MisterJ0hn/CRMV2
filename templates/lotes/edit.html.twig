{% extends 'base.html.twig' %}

{% block title %}Edit Lotes{% endblock %}

{% block body %}
    <h1>Edit Lotes</h1>

    {{ include('lotes/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lotes_index') }}">back to list</a>

    {{ include('lotes/_delete_form.html.twig') }}
{% endblock %}
