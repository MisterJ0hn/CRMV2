{% extends 'base.html.twig' %}

{% block title %}Hello MaxDiasComisionController!{% endblock %}

{% block body %}
{{ form_start(form) }}
<section>
<div class="col-sm-12  col-md-6">
    <div class="card">
        <div class="card-header">
            <h1>Configuraciones Comisiones</h1>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-sm-12 col-md-3">
                    <small class="text-muted">Dias max. comision cobrador</small>
                    {{form_row(form.maxDiasComision,{'label':false,'attr':{'class':'form-control'}})}}
                </div>
            </div>
        </div>
    </div>
</div>
 
      
    <button class="btn btn-primary">{{ button_label|default('Modificar') }}</button>
{{ form_end(form) }}
{% endblock %}
