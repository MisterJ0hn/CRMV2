<form method="post" action="{{ path('pago_tipo_delete', {'id': pago_tipo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pago_tipo.id) }}">
    <button class="btn">Delete</button>
</form>
