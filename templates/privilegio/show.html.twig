{% extends 'base.html.twig' %}

{% block title %}Privilegio{% endblock %}

{% block body %}
    <h1>Privilegio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ privilegio.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('privilegio_index') }}">back to list</a>

    <a href="{{ path('privilegio_edit', {'id': privilegio.id}) }}">edit</a>

    {{ include('privilegio/_delete_form.html.twig') }}
{% endblock %}
