{% extends 'base.html.twig' %}

{% block title %}Materia{% endblock %}

{% block body %}
    <h1>Materia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ materium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('materia_index') }}">back to list</a>

    <a href="{{ path('materia_edit', {'id': materium.id}) }}">edit</a>

    {{ include('materia/_delete_form.html.twig') }}
{% endblock %}
